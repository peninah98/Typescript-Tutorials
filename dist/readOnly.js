"use strict";
const arr = [1, 2, 3, 4, 5, 6, 6, 7, 6, 8];
function square(numbers) {
    return numbers.map((num) => Math.pow(num, 2));
}
console.log(square(arr));
const names = ["Kalisa", "Kamaliza", "Karagwa"];
console.log(names);
